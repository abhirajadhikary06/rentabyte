{
  "name": "rentabyte-contracts",
  "version": "1.0.0",
  "description": "RentAByte smart contracts — Polygon Amoy Testnet",
  "private": true,
  "type": "module",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:amoy": "hardhat run hardhat-scripts/deploy.js --network amoy",
    "deploy:local": "hardhat run hardhat-scripts/deploy.js --network localhost",
    "node": "hardhat node",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^7.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^3.1.12"
  }
}
